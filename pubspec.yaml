name: news_app
description: "App to show news based on newsapi org"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  dio: ^5.4.1
  get_it: ^7.7.0
  flutter:
    sdk: flutter
  rxdart: ^0.28.0
  shared_preferences: ^2.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
